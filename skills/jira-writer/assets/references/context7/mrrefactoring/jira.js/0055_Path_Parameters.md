# Path Parameters

- **issueIdOrKey** (string) - Required - The ID or key of the issue to assign.

- **accountId** (string | null) - Required - The account ID of the user to assign the issue to. Use `null` to unassign the issue. Use `'-1'` to assign to the default assignee.